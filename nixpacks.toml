[phases.setup]
nixPkgs = ["nodejs_20", "python311"]

[phases.install]
cmds = [
  "npm ci --production",
  "python3 -m ensurepip --upgrade && python3 -m pip install --break-system-packages python-docx"
]

[start]
cmd = "node server/index.js"